# Controller
This package is treated as homework assignment which is modified such that it can be ultilized for our project usage.

There are some tunable parameters since you can find in `config/controller_params.yaml`. some of the controller gains can be refined in order to have a better performance.